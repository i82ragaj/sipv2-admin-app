export const environment = {
  production: true,
  // Antes de compilar para producción, sustituye esto por la URL real donde se
  // despliegue SIPV2.AdminAppApi (por ejemplo 'https://api.tu-dominio.com').
  apiUrl: '',
};
